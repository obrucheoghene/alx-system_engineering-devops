#!/usr/bin/env bash
# script to install nginx

# Update package lists
apt-get update

# Install Nginx
apt-get install -y nginx

# Configure Nginx
echo "server {
    listen 80;
    location / {
        echo 'Hello World!';
    }
}" > /etc/nginx/sites-available/default

# Restart Nginx
service nginx restartf
